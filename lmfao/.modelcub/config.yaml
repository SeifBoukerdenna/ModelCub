# ⚠️  WARNING: DO NOT EDIT THIS FILE DIRECTLY!
#
# This file is managed by ModelCub. Editing it manually may cause issues.
# Always use one of these methods to modify configuration:
#
#   CLI:        modelcub project config set <key> <value>
#   Python SDK: project.set_config('key', value)
#   Web UI:     Use the Settings page
#
# Example:
#   modelcub project config set defaults.batch_size 32
#
# Manual edits to this file are NOT supported and may result in
# data corruption or unexpected behavior.

# ModelCub Project Configuration

project:
  name: "lmfao"
  created: "2025-10-21T17:36:17.322436Z"
  version: "1.0.0"

defaults:
  device: "mps"  # Auto-detected: cuda/mps/cpu
  batch_size: 16  # Training batch size
  image_size: 640  # Image resolution (pixels)
  workers: 8  # DataLoader workers
  format: "yolo"  # Internal format (yolo)

paths:
  data: "data"  # Dataset directory
  runs: "runs"  # Training outputs
  reports: "reports"  # Generated reports
